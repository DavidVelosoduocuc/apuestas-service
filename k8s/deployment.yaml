apiVersion: apps/v1
kind: Deployment
metadata:
  name: apuestas-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: apuestas-service
  template:
    metadata:
      labels:
        app: apuestas-service
    spec:
      containers:
        - name: apuestas-service
          image: 407546642824.dkr.ecr.us-east-1.amazonaws.com/apuestas-service:latest
          ports:
            - containerPort: 8005
          envFrom:
            - secretRef:
                name: casino-secrets
          resources:
            requests:
              cpu: "50m"
          livenessProbe:
            httpGet:
              path: /livez
              port: 8005
            initialDelaySeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8005
            initialDelaySeconds: 15
